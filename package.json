{
  "name": "vinterview-monorepo",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api start:dev",
    "dev:web": "pnpm --filter web dev",
    "build:api": "pnpm --filter api prisma:generate && pnpm --filter api build",
    "build:web": "pnpm --filter web build",
    "prisma:generate": "pnpm --filter api prisma:generate",
    "prisma:push": "pnpm --filter api exec prisma db push",
    "db:seed": "pnpm --filter api prisma:seed",
    "db:reset": "pnpm --filter api exec prisma db push --force-reset && pnpm --filter api prisma:seed"
  }
}
